{% set name = "opentelemetry-instrumentation-asyncpg" %}
{% set version = "0.25b1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/opentelemetry-instrumentation-asyncpg-{{ version }}.tar.gz
  sha256: 0a456eb7b76f615a70d3def40fd10e460c04ca9934e0725be4bf0fed7fcdd1fb

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - opentelemetry-api ~=1.3
    - opentelemetry-instrumentation ==0.25b1
    - opentelemetry-semantic-conventions ==0.25b1
    - python >=3.6
    # - opentelemetry-test ==0.24b0
    # - asyncpg >=0.12.0
    # - opentelemetry-test ==0.25b0
    # - asyncpg >=0.12.0
    # - opentelemetry-test ==0.25b1
    # - asyncpg >=0.12.0

test:
  imports:
    - opentelemetry
    - opentelemetry.instrumentation
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/open-telemetry/opentelemetry-python-contrib/tree/main/instrumentation/opentelemetry-instrumentation-asyncpg
  summary: OpenTelemetry instrumentation for AsyncPG
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - mariusvniekerk
